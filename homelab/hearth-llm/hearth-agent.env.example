# Environment for running the Claude Code CLI against Hearth LLM via LiteLLM.
# source this (with the real master key) before running `claude`.
ANTHROPIC_BASE_URL=http://localhost:4000
ANTHROPIC_AUTH_TOKEN=<LITELLM_MASTER_KEY>
ANTHROPIC_MODEL=hearth-llm
ANTHROPIC_SMALL_FAST_MODEL=hearth-llm
