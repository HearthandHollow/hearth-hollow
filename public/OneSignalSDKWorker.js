// Required by the OneSignal Web SDK. Must be served from the site root
// (or the path configured as "Path to service worker files" in OneSignal's
// Web Push settings) so it can control push events for the whole origin.
importScripts('https://cdn.onesignal.com/sdks/web/v16/OneSignalSDKWorker.js');
